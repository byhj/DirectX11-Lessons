
cbuffer cbPerObject :register(b0)
{
	float4x4 MVP;
};

struct VS_OUTPUT 
{
    float4 Pos : SV_POSITION;
    float4 Color : COLOR0;
};

float4 PS(VS_OUTPUT input) : SV_TARGET
{
   return  input.Color;
}