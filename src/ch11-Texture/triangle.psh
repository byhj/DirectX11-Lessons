
struct VS_OUT
{
    float4 Pos : SV_POSITION;
    float2 Tex : TEXCOORD0;
};

Texture2D tex         :register(t0);
SamplerState texState : register(s0);

float4 PS(VS_OUT ps_in) : SV_TARGET
{
   return tex.Sample(texState, ps_in.Tex);
}